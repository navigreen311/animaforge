fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
python-dotenv==1.0.1
httpx==0.27.0

# Numerical core for the X5 avatar pipeline: mesh generation, Gaussian splat
# sampling, glTF/PLY packing, tissue optics and the eye-motion simulation all
# run on CPU with numpy alone. Pure-wheel install, safe on a CI runner.
numpy==1.26.4

pytest==8.2.0
# Bounded on both sides, and both bounds are load-bearing:
#   - 0.23.0 crashes at collection under pytest 8
#     (AttributeError: 'Package' object has no attribute 'obj'). 0.24 is the
#     first release that handles pytest 8's Package collector.
#   - 1.x removes the implicit event loop these tests rely on, turning several
#     into "RuntimeError: There is no current event loop in thread 'MainThread'".
# 0.24.x is the smallest step that fixes collection without changing test
# semantics. .github/workflows/ci.yml was carrying this constraint as a
# workaround; it belongs here.
pytest-asyncio>=0.24,<0.25
