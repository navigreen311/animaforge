fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
python-dotenv==1.0.1
httpx==0.27.0

# Numerical core for the X5 avatar pipeline: mesh generation, Gaussian splat
# sampling, glTF/PLY packing, tissue optics and the eye-motion simulation all
# run on CPU with numpy alone. Pure-wheel install, safe on a CI runner.
numpy==1.26.4

pytest==8.2.0
# pytest-asyncio 0.23.0 crashes on collection with pytest 8.x
# (AttributeError: 'Package' object has no attribute 'obj'), so this needs a
# floor rather than that exact pin.
pytest-asyncio>=0.23.8
