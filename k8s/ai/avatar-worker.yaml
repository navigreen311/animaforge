apiVersion: apps/v1
kind: Deployment
metadata:
  name: avatar-worker
  namespace: animaforge-ai
  labels:
    app.kubernetes.io/name: avatar-worker
    app.kubernetes.io/part-of: animaforge
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: avatar-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: avatar-worker
        app.kubernetes.io/part-of: animaforge
    spec:
      serviceAccountName: avatar-worker
      nodeSelector:
        nvidia.com/gpu: "true"
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
      containers:
        - name: avatar-worker
          image: animaforge/avatar-worker:latest
          env:
            - name: WORKER_TYPE
              value: "avatar"
            - name: QUEUE_NAME
              value: "avatar-generation"
            - name: MODEL_CACHE_DIR
              value: "/models"
          resources:
            requests:
              cpu: "2"
              memory: 8Gi
              nvidia.com/gpu: "1"
            limits:
              cpu: "8"
              memory: 32Gi
              nvidia.com/gpu: "1"
          volumeMounts:
            - name: model-cache
              mountPath: /models
            - name: dshm
              mountPath: /dev/shm
      volumes:
        - name: model-cache
          emptyDir:
            sizeLimit: 40Gi
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 16Gi
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: avatar-worker
  namespace: animaforge-ai
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: avatar-worker
  minReplicas: 0
  maxReplicas: 10
  metrics:
    - type: Pods
      pods:
        metric:
          name: queue_depth_avatar
        target:
          type: AverageValue
          averageValue: "2"
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 30
      policies:
        - type: Pods
          value: 2
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Pods
          value: 1
          periodSeconds: 60
