apiVersion: apps/v1
kind: Deployment
metadata:
  name: video-worker
  namespace: animaforge-ai
  labels:
    app.kubernetes.io/name: video-worker
    app.kubernetes.io/part-of: animaforge
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: video-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: video-worker
        app.kubernetes.io/part-of: animaforge
    spec:
      serviceAccountName: video-worker
      nodeSelector:
        nvidia.com/gpu: "true"
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
        - key: kubernetes.azure.com/scalesetpriority
          operator: Equal
          value: spot
          effect: NoSchedule
      containers:
        - name: video-worker
          image: animaforge/video-worker:latest
          env:
            - name: WORKER_TYPE
              value: "video"
            - name: QUEUE_NAME
              value: "video-generation"
            - name: MAX_BATCH_SIZE
              value: "4"
            - name: MODEL_CACHE_DIR
              value: "/models"
          resources:
            requests:
              cpu: "2"
              memory: 8Gi
              nvidia.com/gpu: "1"
            limits:
              cpu: "8"
              memory: 32Gi
              nvidia.com/gpu: "1"
          volumeMounts:
            - name: model-cache
              mountPath: /models
            - name: dshm
              mountPath: /dev/shm
      volumes:
        - name: model-cache
          emptyDir:
            sizeLimit: 50Gi
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 16Gi
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: video-worker
  namespace: animaforge-ai
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: video-worker
  minReplicas: 0
  maxReplicas: 50
  metrics:
    - type: Pods
      pods:
        metric:
          name: queue_depth_video
        target:
          type: AverageValue
          averageValue: "2"
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 30
      policies:
        - type: Pods
          value: 5
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Pods
          value: 2
          periodSeconds: 60
